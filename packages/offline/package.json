{
  "name": "@kanjijs/offline",
  "version": "1.0.0-alpha.21",
  "description": "Offline-first sync support for Kanji Framework — conflict detection, resolution, and atomic batch application",
  "main": "./dist/index.js",
  "files": ["dist"],
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "bun test",
    "prepublishOnly": "tsc"
  },
  "dependencies": {
    "@kanjijs/core": "workspace:*",
    "@kanjijs/store": "workspace:*",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "bun-types": "1.3.14",
    "@kanjijs/testing": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "6.0.3"
  }
}
